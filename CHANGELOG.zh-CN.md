# 版本记录

这里记录面向用户的重要变化，版本号遵循语义化版本规范。

## [尚未发布]

### 新增

- 全屏视频支持长按半秒后横向拖动进度：从当前播放位置开始相对移动，实时显示目标时间和进度，松手后精确定位；九宫格全屏与 Post 预览打开的系统全屏播放器均支持。
- 在九宫格视频的内部左侧或右侧双击，可后退或前进 5 秒。
- 九宫格视频改用系统播放器表面，并提供由用户主动开启的画中画；App 进入后台时不会自动启动画中画。

### 调整

- 点击侧边翻页仅使用紧凑的最外侧区域，并在按下时显示轻微渐变反馈；手指移出原侧边区域后松开会取消翻页。
- 点击侧边翻页区域现在严格缩小为左右各占屏幕七分之一，为播放器控件留出更多操作空间。
- 左右滑动改为媒体跟随手指移动、露出相邻项目，越过距离或速度阈值后才完成翻页。
- 交互拖页时，相邻视频只显示轻量静态预览；成为当前页后才创建真实播放器。

### 修复

- 从全屏九宫格上滑打开 Post 预览、再返回时，恢复播放的视频不再延迟半秒后被暂停；音频会话只在真正关闭全屏九宫格时清理。
- 全屏媒体现在完整覆盖设备安全区，不再出现上下白边；视频拖动只在长按满半秒后启动，并且不会再阻塞快速侧边点击或上下、左右导航。
- 交互拖页封面现在与正式播放保持相同的居中适配；手势被取消时会自动丢弃临时偏移，不再让九宫格卡在两个媒体之间。
- 九宫格顶部工具恢复原来的贴顶位置，相邻封面在缩略图组件内部强制居中；只有用户先点击系统画中画按钮才允许进入 PiP，普通离开 App 不再自动启动。
- 每次拖动一旦判定为横向或纵向，方向会锁定到手势结束；略带斜向的左右翻页不再误触 Post 预览或退出九宫格。
- 锁定方向现在会保留到 `onEnded` 完成，达到阈值的横向拖动可以真正提交翻页；被系统取消的手势仍会自动清理偏移。
- 翻页后，居中封面会继续覆盖新系统播放器，直到 `isReadyForDisplay` 确认首帧可显示，再无动画移除；无需预载视频即可消除黑色交接闪屏。
- 首帧封面额外保留 20 毫秒完成渲染交接；画中画停止后会恢复原生播放器控件绑定，同时拒绝由 App 进入后台触发的自动画中画。
- 系统播放器触摸区域现在完全由原生视频控件接管；暂时停用可能与播放、暂停、前后跳转、静音和画中画竞争的自定义点击、双击、长按拖进度和视频侧边按钮，仍保留左右拖动翻页，图片侧边点击不变。
- 继续禁止系统播放器发布“正在播放”元数据，同时恢复原生画中画可靠工作所需的音频会话类别。

## [1.2.1] - 2026-07-29

### 新增

- 已索引 Post 支持多选并从本地 Post 索引移除；不会删除已经导出到 Files 或保存到照片的媒体。

### 调整

- “浏览与搜索”默认改为按本机观察到的最近加入书签顺序排列，不再默认按发布时间排序。
- 媒体九宫格只有在手势首先横向移动后才进入纵向连续多选，普通上下拖动继续用于页面滚动。
- 已索引 Post 在拥挤的导航工具栏中改用紧凑的纯图标多选入口。

### 修复

- 媒体多选由横向手势激活后会锁定九宫格滚动位置，后续纵向移动改为选择相邻行，不再滚动页面。

## [1.2.0] - 2026-07-28

### 新增

- 支持 English 与简体中文 UI，可选择跟随系统、English 或简体中文。
- 新增第四个“设置”分页，集中管理语言、存储与缓存、后台音频、默认 Post 预览方式、版本信息和项目链接。
- 为英语和简体中文系统语言分别提供照片“仅添加”权限说明。

### 调整

- 系统语言不是中文时默认使用英语。
- 存储管理入口从书签工具栏迁移到“设置”分页。
- 修复语言切换后缓存的导航返回标题、时长/大小摘要和浏览分页仍显示中文的问题。
- 动图筛选名称缩短为“动图（MP4）”，并从 X 浏览器紧凑工具栏移除媒体大小分析状态。
- 媒体大小分析由单个串行请求改为三个有上限的并发探测，同时继续使用轻量的 HEAD/Range 元数据请求。
- 时长和大小区间的无上限刻度改用不受语言影响的无穷符号。

### 修复

- 移除可能导致 X 浏览器永久白屏的隐藏 WebView 同步实验。现在从书签页点击同步会跳转到可见的 X 浏览器并在那里自动滚动；连续六轮空闲或离开栏目时停止。
- 已索引 Post 与媒体入口统一使用仅箭头的紧凑返回按钮，避免旧语言标题和导航栏拥挤。

## [1.1.0] - 2026-07-27

### 新增

- 保存本机观察到的书签顺序；已索引 Post 与媒体聚合页均可按最近/最早加入书签排序。
- X WebView 不在当前栏目显示时，已启动的书签同步仍可在 App 内继续。
- 图片、动图、视频和全部媒体均支持全屏连续浏览及上一个/下一个切换。
- 三列媒体聚合页支持多选并把所选媒体保存到照片。
- Post 视频支持自适应静音自动播放、系统原生全屏、用户主动开启画中画，以及可选后台音频。

### 改进

- 稳定图片全屏翻页，并移除会产生白闪的切换动画。
- 动图和视频恢复按需加载，不再保留高内存占用的预载缓存。
- 修复较短动图 MP4 的循环播放，并让 Post 预览视频在播放结束后循环。
- 串行处理音频会话切换并收紧播放器生命周期，减少进入、退出和媒体翻页时的停顿。
- 无音轨动图和视频不会仅因播放而打断其他音频。

### 技术限制

- 书签顺序来自 App 在 X 书签时间线中观察到的顺序；此流程无法取得精确的加入书签时间。
- 离开 X 浏览器栏目后可以继续同步，但锁屏或切换到其他 App 时仍可能被 iOS 暂停，并非不受限制的系统后台任务。
- 画中画取决于设备支持和 iOS 设置，且只通过系统播放器控件由用户主动启动。
- Release 中的 IPA 未签名，安装前必须在本机签名。

## [1.0.0] - 2026-07-27

- 首个稳定版本：本地增量书签索引、媒体浏览、单链接下载、保存到照片，以及流式导出到 Files。

[1.1.0]: https://github.com/Xenoxym/x-media-saver/compare/v1.0.0...v1.1.0
[1.0.0]: https://github.com/Xenoxym/x-media-saver/releases/tag/v1.0.0
[尚未发布]: https://github.com/Xenoxym/x-media-saver/compare/v1.2.1...HEAD
[1.2.1]: https://github.com/Xenoxym/x-media-saver/compare/v1.2.0...v1.2.1
[1.2.0]: https://github.com/Xenoxym/x-media-saver/compare/v1.1.0...v1.2.0
